services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: oxwar
      POSTGRES_USER: oxwar
      POSTGRES_PASSWORD: oxwar
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
